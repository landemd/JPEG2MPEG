这个错误是由于相对导入超出了顶级包的范围。在Python中，当直接运行一个模块时，该模块被认为是顶级模块，不能使用相对导入来引用上级模块。
由于Python的相对导入问题导致的。当直接运行脚本时，Python无法正确解析相对导入路径。让我们修复这个问题，使用绝对导入方式重新组织代码。

解决这个问题的方法有两种：

1、将整个项目作为一个包，然后通过-m选项运行。

2、修改导入语句为绝对导入。

由于我们最终需要将代码打包成zip文件，并且希望代码能够直接运行，我们选择第二种方法：修改导入语句为绝对导入。